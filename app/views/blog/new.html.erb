<h1>New blog post</h1>

<%= form_for Post.new, :html => { :multipart => true }, :url => { :action => "create" } do |f| %>
    <%= f.text_field :title %>
    <br />
    <%= f.text_area :body %>
    <br />
    <%= f.file_field :photo %>
    <br />
    <%= f.submit "Save" %>
<% end %>
